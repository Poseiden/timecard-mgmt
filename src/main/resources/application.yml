server:
  port: 8080
spring:
  profiles:
    active: development
  datasource:
    url: jdbc:mysql://localhost:33306/working_hour_mgmt
    username: root
    password: admin

  flyway:
    enabled: true

  jpa:
    hibernate:
      ddl-auto: none
